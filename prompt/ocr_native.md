This image is one page of Bourbaki, Elements of Mathematics. Transcribe the whole page as Markdown with LaTeX, in the language it is printed in, translating nothing.

The page is clean type and the reason it is being read from a picture is that something on it did not survive the text layer: a commutative diagram, a matrix or a cases whose rows ran together, a glyph that was dropped, a delimiter that lost its partner. Read what is printed, all of it, and put those back.

Copy the letter that is printed rather than the one you expect. A Lie algebra, an ideal of one and a Cartan subalgebra are drawn in the fraktur face, so write \mathfrak{g}, \mathfrak{h}, \mathfrak{t}, and never the plain letter. A fraktur letter stays fraktur wherever it stands, a subscript included: the function attached to the Lie algebra \mathfrak{g} is \lambda_{\mathfrak{g}} and never \lambda_g, and the same goes for \delta_{\mathfrak{g}}, \pi_{\mathfrak{g}} and \lambda_{\mathfrak{h}}. A letter drawn in the script face is \mathscr and never \mathcal, so the space of continuous functions is \mathscr{C} and the algebra of endomorphisms is \mathscr{L}.

Bold roman is for two things and nothing else. The number sets, never blackboard bold: \mathbf{Z}, \mathbf{Q}, \mathbf{R}, \mathbf{C}, \mathbf{N}, \mathbf{F}_q, never \mathbb. And the classical groups, which are bold and not upright roman: \mathbf{SU}(2,\mathbf{C}), \mathbf{SO}(3,\mathbf{R}), \mathbf{U}(n), \mathbf{O}(n), \mathbf{Sp}(2n), \mathbf{GL}(n,k), never \mathrm{SU} and never a plain SU. Everything else is ordinary italic even when the letter is one that a number set uses. Bourbaki writes the module Z(\lambda-\rho) with an italic Z and it is not the integers; a root system is R, its root lattice is Q(R) and its weight lattice is P(R), all italic; a space, a group, a subspace or a map named by a plain capital stays a plain capital. Bold a letter only where the ink on the page is visibly heavier than the italic around it.

The curly rho is \varrho and the straight one is \rho. The script l is \ell. The inequality signs are the slanted \leqslant and \geqslant. A bar drawn through a relation is part of it, so read \not= and \not\subset where the bar is there and = and \subset where it is not. Angle brackets around a pair are \langle and \rangle. An accent belongs to the letter it is drawn over, and a mark drawn wide enough to span an expression is \widehat, \widetilde or \overline rather than the narrow form.

The first line of the page is the running head, printed above the text block. Write it verbatim on its own first line, keeping the page label such as A I.24 and the locator such as § 2.5 exactly as printed, and keep it even when it is only a page number. Keep § and no. as they are printed and do not expand them. The head is set across the width of the page, with the locator at one margin, the title in the middle and the folio at the other, and it is still one line: write it as one line with single spaces between those parts, never as two or three lines.

A numbered section heading, printed in small capitals over the text, is a Markdown heading and not bold prose: write ### 4. CENTRAL FUNCTIONS ON G AND FUNCTIONS ON T. A statement head is printed in small capitals and followed by a spaced em rule, and that one is bold with the rule kept: **PROPOSITION 3.** —, **THEOREM 1.** —, **COROLLARY.** —, **Remark.** —. EXERCISES is a heading on its own line.

A commutative diagram is a LaTeX array with its arrows in it, never a description in words and never rows written one after another. A matrix, a cases and a table are LaTeX too. A numbered display keeps its number as \tag{11}. Footnotes sit below a short rule, which is three hyphens on its own line.

If part of the page is illegible, write ⟪illegible⟫ at that point rather than inventing content. Do not guess a digit, a subscript or an index you cannot read.

Give me the page and nothing else. No preamble, no closing remark, no code fence.
