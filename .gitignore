# Specs and design notes live outside this repo, they carry host names and keys
docs/
notes/

# Build artifacts
bin/
dist/
coverage.out
*.test

# Local config, route files, credentials
routes.json
config.toml
*.pem
.env

# Scratch
work/
images/
*.log

# Source books and rendered pages. Neither repo carries them: the PDFs are not
# ours to publish and a page image is a megabyte that the Markdown replaces.
*.pdf
*.png
*.jpg
*.jpeg
*.tif
*.tiff
*.ppm
*.pgm

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/
